
<div class="flex-container {{zoomClass}}">

    {{#each sortedAndFilteredGalleryItems as |gallery-item|}}
        {{ gallery-item 
            gallery-item=gallery-item
        }}
    {{/each}}

    {{yield}}
</div>
